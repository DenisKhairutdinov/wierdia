const options = {
    id: 725848418,
    autoplay: true,
    muted: true,
    loop: true,
    responsive: true,
    unmute_button: false,
    keyboard: false,
    controls: false,
};

const player = new Vimeo.Player('article-cover__video', options);